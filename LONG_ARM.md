# Long arm

Autonomous continue is allowed only along LONG_ARM.md.
Curiosity is not a transition.
Unfreezing a diet knob is a halt.

## Status

state: halt
next legal node: none

Hand-off origin finished. 0 of 3 k-NN seeds. Fail-predator: they lived on
hemolymph (rasp × find), killed arrivals, never held stolen share. gut_probe
stayed untrained. Fruit-forever stolen share 0.00081 < 0.05.

Do not raise gut_probe payoff. Do not restamp vial_morsus `69f4f6d` or
vial_culex `ddb73a8`. Do not average with fa819c7.

A later question is a new repo or a named lock, not a flag pile.

Do not pin GraphForge.

## Frozen

t_snap=200, M0=200, a0=8, generations=2500, k=3, phi_max=0.25,
h0=0.22, s0=1.20, z_max=3.0.
